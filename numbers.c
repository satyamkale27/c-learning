#include<stdio.h>
#include<math.h>

int main()
{

printf("%f", pow(2, 3) ); // it is power function it gives 2^3
printf("%f", sqrt(36) );  // it shows square root 

printf("%f", floor(36.669) ); // it round offs the number

    return 0;
}